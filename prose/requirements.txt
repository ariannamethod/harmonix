llama-cpp-python>=0.2.0  # TinyLlama inference via llama.cpp (local mode)
huggingface-hub>=0.19.0  # HuggingFace fallback for weights
requests>=2.31.0  # HuggingFace API wrapper (hf_api.py)
numpy>=1.24.0  # Emergent layer (prosebrain, proserae, phase_transitions)
scikit-learn>=1.3.0  # For embedding/clustering in dream_prose
